const Lambda = require('../../src/index');
const should = require('should');

describe.only('@sage/bc-services-lambda.index', function(){
    this.timeout(10000);
    // placeholder
    it('should force file to be evaluated', () => {
        // const lambda = new Lambda({ region: 'eu-west-1' });
        //
        // return lambda.invoke({ functionName: 'papi-dev02-eu-west-1-BatchStatus', payload: '' })
    });
});
