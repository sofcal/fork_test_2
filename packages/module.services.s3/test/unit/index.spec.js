const index = require('../../src/index');
const should = require('should');

describe('internal.services.s3.index', function(){
    // ensures all files are evaluated for test coverage
    it('should do nothing', () => {

    });
});