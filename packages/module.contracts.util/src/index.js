'use strict';

const createFilters = require('./createFilters');
const extend = require('./extend');
const filter = require('./filter');
const validateType = require('./validateType');

module.exports = { createFilters, extend, filter, validateType };
