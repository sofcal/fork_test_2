{
  "name": "@sage/sfab-s2s-jwt-legacy-wrapper",
  "version": "1.1.136",
  "author": "Sage UK",
  "license": "ISC",
  "description": "Legacy Wrapper for components used within Banking Cloud. These modules are installed locally using npm > 6 and then copied into the Banking Cloud solution to reduce dependency overhead (due to BC's use of npm 2.15.1)",
  "main": "lib/index.js",
  "private": true,
  "directories": {
    "lib": "lib"
  },
  "publishConfig": {
    "registry": "https://sage.myget.org/F/npm/npm/",
    "access": "public"
  },
  "dependencies": {
    "@sage/bc-common-resources": "^1.1.131",
    "@sage/bc-common-statuscodeerror": "^1.1.131",
    "@sage/bc-contracts-bank": "^1.1.136",
    "@sage/bc-contracts-lock": "^1.1.131",
    "@sage/bc-contracts-predictedaction": "^1.1.136",
    "@sage/bc-contracts-quarantine": "^1.1.136",
    "@sage/bc-contracts-rule": "^1.1.136",
    "@sage/bc-contracts-transaction": "^1.1.136",
    "@sage/bc-jsonschema-to-statuscodeerror": "^1.1.131",
    "@sage/bc-processing-rules": "^1.1.136",
    "@sage/bc-services-parameter": "^1.1.136",
    "@sage/bc-services-step": "^1.1.136",
    "@sage/bc-services-validators": "^1.1.131",
    "@sage/sfab-s2s-jwt-authenticator": "^1.1.136",
    "@sage/sfab-s2s-jwt-cache": "^1.1.136",
    "@sage/sfab-s2s-jwt-endpoint-store": "^1.1.136",
    "@sage/sfab-s2s-jwt-issuer": "^1.1.136",
    "@sage/sfab-s2s-jwt-jwks": "^1.1.136",
    "@sage/sfab-s2s-jwt-kid": "^1.1.131",
    "@sage/sfab-workers-clam-av": "^1.1.136"
  },
  "devDependencies": {
    "@sage/bc-debug-utils": "^1.1.131",
    "eslint": "^4.11.0",
    "eslint-config-airbnb": "^16.1.0",
    "eslint-plugin-import": "^2.8.0",
    "eslint-stats": "^1.0.0"
  },
  "scripts": {
    "buildwrapper": "npm i && babel src -d lib --copy-files && npm run clean && npm i --production",
    "clean": "rimraf ./coverage/ && rimraf ./.nyc_output/ && rimraf ./node_modules/",
    "test": "NODE_ENV=test nyc --cache false --x test/ mocha -b --colors \"./test/unit/**/*.spec.js\"",
    "testwatchunit": "NODE_ENV=test mocha -b -w --colors \"./test/unit/**/*.spec.js\"",
    "testwatchuint": "NODE_ENV=test mocha -b -w --colors \"./test/int/**/*.spec.js\"",
    "testint": "NODE_ENV=test nyc --x lib/_legacy --x test/ mocha -b --colors --reporter spec \"./test/int/**/*.spec.js\"",
    "cover": "NODE_ENV=test npm run test && nyc report --reporter=lcov",
    "report": "open coverage/lcov-report/index.html",
    "cover:report": "npm run cover && npm run report",
    "lint": "eslint ./src/**",
    "build": "",
    "watch": "babel --watch src --out-dir lib --copy-files",
    "unlock": "rimraf package-lock.json"
  }
}
