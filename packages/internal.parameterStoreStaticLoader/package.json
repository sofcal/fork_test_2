{
  "name": "internal-parameterstorestaticloader",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "main": "lib/index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "clean": "rimraf ./coverage/ && rimraf ./.nyc_output/ && rimraf ./node_modules/",
    "test": "NODE_ENV=test nyc --cache false --x test/ mocha -b --colors \"./test/unit/**/*.spec.js\"",
    "cover": "NODE_ENV=test npm run test && nyc report --reporter=lcov",
    "report": "open coverage/lcov-report/index.html",
    "int": "NODE_ENV=test nyc --x test/ mocha -b --colors --reporter spec \"./test/int/**/*.spec.js\"",
    "lint": "eslint ./src",
    "unlock": "rimraf package-lock.json"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "istanbul": "^1.1.0-alpha.1",
    "mocha": "^4.0.1",
    "nyc": "^11.8.0",
    "should": "^13.2.1",
    "sinon": "^4.1.3"
  }
}
